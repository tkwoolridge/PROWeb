@using PROWeb.Components.Person.Filters;
@using PROWeb.Components.EligiblePolls.Views;
@using PROWeb.Components.EligiblePolls.ViewModels;
@inherits PROListComponent<FilterModel, EligibleViewModel>

<style>
    .eligible-grid td.vertical-aligned-cell {
        vertical-align: top;
    }

    .eligible-grid th.align-center {
        text-align: center;
    }
</style>


<TelerikGrid Data="@Data"
             @bind-Page="@Page"
             PageSize="@PageSize"
             Class="eligible-grid"
             Height="100%"
             TItem="EligibleViewModel"
             SelectedItemsChanged="@OnSelectionChanged"
             SelectionMode="@(EnableSelection ? GridSelectionMode.Single : GridSelectionMode.None)"
             Pageable="true">
    <GridColumns>
        @if(EnableSelection)
        {
            <GridCheckboxColumn Width="25px" SelectAll="false" />
        }
        <GridColumn HeaderClass="align-center" Width="30%" Title="BIRTHS REGISTER" OnCellRender="@( (GridCellRenderEventArgs e) => e.Class = "vertical-aligned-cell" )">
            <Template>
                @{
                    var eligible = (EligibleViewModel) context;
                    <CascadingValue Value="@eligible">
                        <BirthView />
                     </CascadingValue>
                }
            </Template>
        </GridColumn>
        <GridColumn HeaderClass="align-center" Width="30%" Title="STATUS REGISTER" OnCellRender="@( (GridCellRenderEventArgs e) => e.Class = "vertical-aligned-cell" )">
            <Template>
                @{
                    var eligible = (EligibleViewModel)context;
                    <CascadingValue Value="@eligible">
                        <ImmigrationView/>
                    </CascadingValue>
                }
            </Template>
        </GridColumn>
        <GridColumn HeaderClass="align-center" Width="40%" Title="DRIVERS REGISTER" OnCellRender="@( (GridCellRenderEventArgs e) => e.Class = "vertical-aligned-cell" )">
            <Template>
                @{
                    var eligible = (EligibleViewModel)context;
                    <CascadingValue Value="@eligible">
                        <DriverLicenseView />
                    </CascadingValue>
                }
            </Template>
        </GridColumn>
    </GridColumns>
</TelerikGrid>