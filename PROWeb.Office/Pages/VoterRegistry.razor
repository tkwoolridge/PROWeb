@page "/"
@using PROWeb.Components.Navigation
@using PROWeb.Components.Common
@using PROWeb.Components.Person.Filters
@using PROWeb.Components.ViewModels.Person
@using PROWeb.Office.Components.Voters
@using PROWeb.Office.Components.Voters.Filters
@using PROWeb.Office.ViewModels.Voters;
@using Telerik.Blazor.Components.PanelBar
@using PROWeb.Office.Components.VoterRegistry
@inherits PRORegistryLayout<PersonFilterViewModel, ListVoterViewModel>

<FilterListLayout>
    <Filter>
        <ExpansionPanel>
            <Header>
                <strong>Filter:</strong>
            </Header>
            <ToolBar>
                <TelerikButtonGroup SelectionMode="@ButtonGroupSelectionMode.Single">
                    <ButtonGroupToggleButton SelectedChanged="@OnSimpleFilterSelect" Selected="@SimpleFilterSelected">Simple Search</ButtonGroupToggleButton>
                    <ButtonGroupToggleButton SelectedChanged="@OnAdvancedFilterSelect" Selected="@AdvancedFilterSelected">Advanced Search</ButtonGroupToggleButton>
                </TelerikButtonGroup>
            </ToolBar>
            <Content>
                @FilterTemplate
            </Content>
        </ExpansionPanel>
    </Filter>
    <List>
        <VoterList @ref="ListRef" />
    </List>
</FilterListLayout>

@code
{
    private RenderFragment AdvancedFilterTemplate => @<VoterFilterAdvanced @ref="@FilterRef"></VoterFilterAdvanced>;

    private RenderFragment SimpleFilterTemplate => @<PersonFilterSimple @ref="@FilterRef" ></PersonFilterSimple>;
}