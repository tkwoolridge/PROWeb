@using PROWeb.Office.ViewModels.Candidates;
@using PROWeb.Components.Person.Filters;
@inherits PROListComponent<FilterModel, CandidateViewModel>

<TelerikGrid Data="@Data">
    <GridColumns>
        <GridColumn Title="Birth register">
            <Template>
                @{
                    var candidate = (CandidateViewModel) context;
                }
            </Template>
        </GridColumn>
        <GridColumn Title="Status register">
            <Template>
                @{
                    var candidate = (CandidateViewModel)context;
                }
            </Template>
        </GridColumn>
        <GridColumn Title="Driver licenses register">
            <Template>
                @{
                    var candidate = (CandidateViewModel)context;
                }
            </Template>
        </GridColumn>
    </GridColumns>
</TelerikGrid>